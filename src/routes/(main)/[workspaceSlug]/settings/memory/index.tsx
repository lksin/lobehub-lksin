'use client';

import Page from '@/routes/(main)/settings/memory';

const WorkspaceMemorySetting = () => <Page />;

WorkspaceMemorySetting.displayName = 'WorkspaceMemorySetting';

export default WorkspaceMemorySetting;
