'use client';

import Page from '@/routes/(main)/settings/messenger';

const WorkspaceMessengerSetting = () => <Page />;

WorkspaceMessengerSetting.displayName = 'WorkspaceMessengerSetting';

export default WorkspaceMessengerSetting;
